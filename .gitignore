# Dependencies
node_modules/

# Environment / secrets
.env
.env.local
.env.*.local

# Database / runtime state
*.db
*.db-journal
*.db-wal
*.db-shm
.tavernos/secrets.json
.tavernos/data/

# Note: packages/core/src/ contains infrastructure source code (LLM client,
# storage, crypto, types) licensed under GPL v3. It is intentionally public.
# The proprietary core writing engine is only in packages/core/dist/.
# Build caches (NOT dist/ - dist/ contains the compiled core engine)
*.tsbuildinfo
.vite/
coverage/
.turbo/

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# Temp / backups
temp/
*.bak
*.bak_*
*.orig
*.tgz
reference/
node_modules_old_*/

# Electron installer output (NOT the built app dist/)
release/
electron/build/node/
electron/runtime-modules/

# Test files
packages/studio/test_*.mjs
test_*.py
__pycache__/
