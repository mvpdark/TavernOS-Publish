// Type definitions — re-exported from @tavernos/core to avoid duplication.

import type { AssetKind } from "@tavernos/core";

export type { Asset, AssetKind, AssetCatalog } from "@tavernos/core";

export type TabKind = AssetKind | "all";
